# 当前未完成清单

当前状态以 `2026-06-10` 为准。

## 1. 正在进行

当前账号治理主线已收口，没有正在进行的 P0 级账号治理项。

## 2. 真未做

### 2.1 产品增强待排期

#### 2.1.1 长周期学习指标

- 连续学习天数
- 周 / 月复习量
- 单词 / 知识点分线趋势
- 更长期负载预测

#### 2.1.2 复习体验继续打磨

- 完成收尾页
- 跳过 / 回退 / 重做
- 更顺手的移动端布局
- 更明确的过程反馈

### 2.2 质量与工具债

- 前端仍然没有自动化测试
- 后端缺少稳定可复用的 Maven Wrapper；当前可用 `D:\apache-maven-3.9.12`
- Codex 沙箱内 `javac --release 21` 仍会报 `致命的エラー: コンパイラ・リソースを閉じることができません`
- 本机 IDEA Maven 与 `D:\apache-maven-3.9.12` 均使用 JDK 21，并已通过 `compile` 和 `test`，当前不按项目编译失败处理
- 更多 controller / service 回归覆盖仍未补齐

## 3. 已完成但容易被误判的项

下面这些能力已经落地，不应再按“缺失功能”重复立项：

- 本地注册 / 登录 / 登出
- Session cookie 显式配置、CSRF token 自动携带、bootstrap 默认关闭
- 登录失败限流 / 锁定策略：默认 5 次失败锁定 15 分钟
- 认证安全审计事件：登录成功、登录失败、账号锁定、账号禁用、登出
- 安全告警策略：近 24 小时重复登录失败、账号锁定、禁用账号登录尝试派生告警
- 审计事件后台查询 / 管理端展示
- 用户治理后台 MVP：用户列表 / 搜索 / 详情、启用 / 禁用、后台重置密码、关键操作审计
- 导出工作流预检查：创建前校验模板、目标日期内容和导出上下文，空日期不再生成空文件
- 单词学习闭环
- 知识点学习闭环
- `/dashboard` 统一工作台
- `/weak-items` 薄弱项入口
- `/backups` 本地备份 / 本地恢复 MVP
- 管理员角色基础与 `/admin` 访问守卫

如果继续做，应按增强项描述，不按从零建设描述。
