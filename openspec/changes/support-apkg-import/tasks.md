## 1. Backend Import Pipeline

- [x] 1.1 为后端增加读取 `.apkg` 集合数据库所需依赖，并整理统一的词条导入入口
- [x] 1.2 实现 `.apkg` 解包、Anki note 字段映射、内容清洗和与现有去重/落库逻辑的复用
- [x] 1.3 调整词条导入控制器与错误处理，保持 CSV 兼容并为不支持的包格式返回明确错误

## 2. Frontend Entry

- [x] 2.1 更新词库页面上传入口与提示文案，允许用户上传 `.csv` 或 `.apkg`
- [x] 2.2 保持现有导入结果展示逻辑兼容 `.apkg` 导入

## 3. Documentation

- [x] 3.1 更新系统使用文档，补充 `.apkg` 导入步骤、字段兼容规则和当前限制
